#include <stdio.h>
int main(void)
{
    int a,b,c,d,e; //변수 선언
    
    scanf("%1d%1d%1d%1d%1d",&a,&b,&c,&d,&e); //입력 값을 한자리 수로 제한한다 , 5자리 수를 입력한다
    
    printf("[%d]\n",a*10000); //각 자리수를 출력 , 가령 75432 를 입력받았을 경우 , [70000] [5000] [400] [30] [2] 를 각각 출력
    printf("[%d]\n",b*1000);
    printf("[%d]\n",c*100);
    printf("[%d]\n",d*10);
    printf("[%d]\n",e);
}
