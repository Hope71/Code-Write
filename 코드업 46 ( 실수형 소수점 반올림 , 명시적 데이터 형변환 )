#include<stdio.h>
int main(void)
{
int a,b,c;

scanf("%d %d %d",&a,&b,&c);

printf("%d\\n",a+b+c);
printf("%.1f\\n",(float)(a+b+c)/3); /* 평균은 소수점 이하 둘째 자리에서 반올림해서
소수점 이하 첫째 자리까지 출력한다 라는 조건이 주어졌으므로 명시적 데이터 형변환으로
정수형 변수를 실수형 변수로 계산한다 */

}


(float)(a+b+c)/3 인 경우에 쓰레기 값이 나오는 이유 
